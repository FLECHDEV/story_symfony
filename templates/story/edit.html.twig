{# {% extends 'base.html.twig' %}
{% block title %}Edit Story
{% endblock %}

{% block body %}
	<h1>Edit Story</h1>

	{{ include('story/_form.html.twig', {'button_label': 'Update'}) }}


	{{ include('story/_delete_form.html.twig') }}
{% endblock %} #}

{% extends 'base.html.twig' %}

{% block title %}
    Edit Story
{% endblock %}

{% block body %}
    <div class="container" style="padding:50px 0">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <h1 class="text-center">Modifier le titre du votre story</h1>
				{{ include('story/_form.html.twig', {'button_label': 'Update'}) }}
            </div>
        </div>
    </div>

	{# {{form_start(form)}}
	{{form_row(form.name)}}
	{{form_end(form)}} #}
{% endblock %}

